{
    "$schema": "https://railway.app/v2/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd backend_laravel && php artisan serve --host=0.0.0.0 --port=$PORT",
        "rootDirectory": "./"
    }
}