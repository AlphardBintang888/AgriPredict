{
    "build": {
        "builder": "NIXPACKS",
        "env": {
            "NODE_VERSION": "20",
            "NIXPACKS_METADATA": "backend_laravel"
        }
    },
    "deploy": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
        "rootDirectory": "backend_laravel"
    }
}